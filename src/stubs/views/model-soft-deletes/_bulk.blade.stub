<?php
    $actions = [];

    if (auth()->user()->can('delete_{{pluralSnake}}')) {
        $actions['delete'] = __('Delete');
    }
?>

@include('admin.components.bulk', ['actions' => $actions, 'model' => '{{pluralSnake}}'])