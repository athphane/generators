        $rules += [
            // rules
        ];

        // unique definitions
        if (${{singularSnake}} = $this->getUserBeingEdited()) {
            // unique ignores
        } else {
            // requireds
        }

        // unique rule insertions
        return $rules;
