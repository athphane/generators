        $rules = [
            // rules
        ];

        // unique definitions
        if (${{singularSnake}} = $this->route('{{singularSnake}}')) {
            // unique ignores
        } else {
            // requireds
        }

        // unique rule insertions
        return $rules;
